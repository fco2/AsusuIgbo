<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="languageInfo"
            type="com.asusuigbo.frank.asusuigbo.models.LanguageInfo" />
        <variable
            name="clickListener"
            type="com.asusuigbo.frank.asusuigbo.adapters.chooselang.ChooseLangClickListener" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorWhite"
        android:padding="15dp"
        android:onClick="@{() -> clickListener.onClick(languageInfo)}"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <TextView
            android:id="@+id/language"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/colorPrimary"
            android:textSize="16sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:gravity="start|center_vertical"
            app:Language="@{languageInfo}"
            android:text="@string/select_language"
            android:fontFamily="@font/galindo"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
